<Page x:Class="WpfLayoutUIOefenblad.Exercises.StackPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfLayoutUIOefenblad.Exercises"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="800"
      Title="StackPanel">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="#EEEFFD" BorderBrush="#6B6CB0"  Padding="12" Margin="20">
            <!-- TODO: PAS HIERONDER DE XAML AAN -->
            <Grid MaxWidth="500">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <Label Content="Postcode:" FontWeight="Bold" Margin="0,0,10,0" Padding="0,5"/>
                    <TextBox Text="5330" BorderBrush="LightBlue" BorderThickness="1" Height="30" VerticalContentAlignment="Center"/>
                    <TextBlock Text="b.v. 1234" TextAlignment="Right" FontSize="10" FontStyle="Italic" Padding="5" Foreground="#666"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="20,0,0,0">
                    <Label Content="Gemeente:" FontWeight="Bold" Margin="0,0,10,0" Padding="0,5"/>
                    <TextBox Text="Polivache" BorderBrush="LightBlue" BorderThickness="1" Height="30" VerticalContentAlignment="Center"/>
                    <TextBlock Text="b.v. Lier" TextAlignment="Right" FontSize="10" FontStyle="Italic" Padding="5" Foreground="#666"/>
                </StackPanel>

            </Grid>
        </Border>

    </Grid>
</Page>
